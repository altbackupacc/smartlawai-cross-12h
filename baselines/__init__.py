"""M7 baselines: evaluation-time only, never imported by served code
(pipeline.py, api/main.py, ui/app.py). Mirrors eval/ and train/'s shape as a
top-level package rather than living under src/smartlawai/."""
