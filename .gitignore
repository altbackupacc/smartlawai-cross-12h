__pycache__/
*.pyc
*.egg-info/
.venv/
.env
.smartlaw_local/
*.duckdb
*.bin
*.idmap
ckpt-*/
mistral-*-lora/
.ipynb_checkpoints/
node_modules/
dist/
.pytest_cache/
.ruff_cache/

# M1 data foundation -- large and/or fully regenerable from data/*.py + frozen splits
data/raw/
data/processed/
!data/processed/near_duplicate_pairs.json
data/ocr_wer_gold/pages/

# M6 -- generated perturbation training data: large and fully regenerable from
# data/perturbations/generate_perturbations.py + its fixed seed. The generator
# and its README are committed; the output is not. eval/gold/*.jsonl and
# eval/results/*.json are deliberately NOT ignored (OPS.md §4: keep forever, in git).
data/perturbations/perturbations.jsonl
eval/annotations/
