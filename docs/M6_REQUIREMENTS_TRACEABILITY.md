# M6 Requirements Traceability Matrix

Produced by a full independent re-read of `CLAUDE.md`, `PLAN.md` (§M6 and cross-
referenced sections), `RESEARCH.md` (§1–12, full), `OPS.md` (§1–10, full),
`M6_ONBOARDING.md` (§1–18, full), `M1_ONBOARDING.md` §7–9, `M4_ONBOARDING.md`
§6, and the two reference images. Every row below was checked against the
actual repository state on disk, not against the earlier completion report's
own claims — that report is treated as untrusted per the audit instructions.

**Image caveat, stated plainly**: the two reference images cannot be re-opened
in this turn (no image was re-attached). Rows sourced from them cite the
transcription recorded in `docs/M6_IMPLEMENTATION_ANALYSIS.md` §9, made during
the original viewing. This is a genuine limitation of this audit, disclosed
rather than papered over.

Status legend: NOT STARTED · PARTIALLY IMPLEMENTED · IMPLEMENTED ·
IMPLEMENTED BUT INCORRECT · NOT REQUIRED · BLOCKED

| ID | Source | Exact Requirement | Status | File(s) | Evidence | Gap/Issue |
|---|---|---|---|---|---|---|
| R1 | M6_ONBOARDING §4 | Move `src/smartlawai/eval/metrics.py` → `eval/metrics.py`; update the two test importers; delete the old package | IMPLEMENTED | `eval/metrics.py`, `tests/test_metrics.py:14`, `tests/test_local_backend.py:50` | `git mv` performed; `test_old_src_eval_package_is_gone` passes; grep confirms zero remaining `smartlawai.eval` imports | None |
| R2 | PLAN.md §M6, M6_ONBOARDING §8 | Delete the hand-rolled ROUGE-L; replace with `rouge-score` | IMPLEMENTED | `eval/metrics.py:73-90` (`rouge_scores`) | `def rouge_l(` count in `eval/metrics.py` is 0 (asserted by `test_old_src_eval_package_is_gone` no — actually by `test_top_level_eval_package_is_where_metrics_now_live`); uses `rouge_scorer.RougeScorer` | None |
| R3 | RESEARCH.md §5 | Retrieval: Recall@{5,10,20}, MRR, nDCG@10 | IMPLEMENTED | `eval/metrics.py:194-262` (`recall_at_k`, `mean_reciprocal_rank`, `ndcg_at_k`, `retrieval_metrics`) | nDCG hand-verified against a worked DCG/IDCG example in `tests/test_metrics.py::test_ndcg_reversed_ranking_matches_hand_computation` | None. Precision@K also kept (pre-existing, sanctioned by onboarding §8 "kept") |
| R4 | RESEARCH.md §5 | Summarisation: ROUGE-1/2/L, BERTScore, Faithfulness (HHEM-2.1, claim-level), Extractiveness | PARTIALLY IMPLEMENTED (before fix), corrected below | `eval/metrics.py`, `eval/extractiveness.py`, `eval/run_eval.py::score_groundedness` | ROUGE/BERTScore/Extractiveness functions correct. **"Faithfulness, claim-level" was wired to read the gold label as the system's own judgment (tautological — see G1 in gap analysis), corrected in this audit to run the pipeline's own verifier.** | Fixed in Step 6 below |
| R5 | RESEARCH.md §5 | QA: claim-level groundedness (InLegalNLI + HHEM-2.1, both reported, I11); Citation validity rate (registry resolution); Repealed-citation rate (registry + in-force interval) | IMPLEMENTED BUT INCORRECT (before fix) | `eval/run_eval.py::score_groundedness`, `score_repealed` | Groundedness computed the gold set's own label distribution (`0.5000` exactly = 20/40 by construction). Citation/repealed rates computed from `RepealedItem.as_registry_result()`, i.e. the gold label reinterpreted as a fake registry answer (`0.7647` exactly = 26/34 by construction). Both are dataset-composition artifacts presented as measured system numbers — a CLAUDE.md I2 violation in spirit even though each individual metric function correctly returns `None` when truly given nothing. **InLegalNLI is not implemented at all — correctly so, it is M8a's deliverable, not M6's; M6's job is only to build the training-data generator and the metric slots, which is done.** | CRITICAL — fixed in Step 6 |
| R6 | RESEARCH.md §5 | Judge: agreement with human labels (Cohen's κ), head-to-head vs HHEM-2.1, per-perturbation-type detection rate, external generalisation (ClaimRAG-Law) | PARTIALLY IMPLEMENTED | `eval/annotate/agreement.py::cohen_kappa`, `eval/metrics.py::per_perturbation_detection_rate`, `eval/gold/external/PROVENANCE.md` | κ function exists and is tested against worked examples. Per-perturbation detection rate function exists and is tested. **Neither was actually wired into `run_eval.py`'s live run before this audit** — `_human_metrics()` only reports human-rater agreement (which is `None`, correctly, since no real study ran), not judge-vs-human agreement. Head-to-head vs HHEM-2.1 is `NOT APPLICABLE` — HHEM/InLegalNLI don't exist (M5/M8a not started). ClaimRAG-Law confirmed unavailable, documented. | Judge-vs-human agreement now wired via the Step 6 fix; head-to-head vs HHEM stays `NOT APPLICABLE` until M5/M8a land |
| R7 | RESEARCH.md §5 | Refusal: Precision/recall on labelled OOS | IMPLEMENTED | `eval/metrics.py::refusal_metrics`, `eval/run_eval.py::run_oos` | Genuinely exercises `Pipeline.ask()` with real questions from `oos.jsonl`, compares real `decision` against gold label. Verified: this is the one metric section with NO fabrication issue, because it correctly routes through `.ask()` | None |
| R8 | RESEARCH.md §5 | Calibration: ECE + reliability diagram | IMPLEMENTED | `eval/stats.py::expected_calibration_error`, `eval/run_eval.py::calibration_metrics` | Deliberately reports `unavailable` without an independent gold-correctness label, to avoid circularity (scoring the gate's own ACCEPT/REFUSE as "correct" would let confidence predict itself). This was caught and fixed during the *original* implementation session, before this audit — verified still correct on re-inspection | None |
| R9 | RESEARCH.md §5 | Cost/latency: tokens, USD, p50/p95 per stage, via `PipelineTrace` | IMPLEMENTED | `eval/metrics.py::latency_summary`, `percentile` | Computed from real `trace.stages[].duration_ms`, genuinely measured wall-clock per run | Token/USD cost is always `0.0` in `trace.cost` because the stub pipeline hardcodes it (`pipeline.py:83,105`) — not an M6 defect, correctly reflects pipeline.py's current (unmodified) behavior |
| R10 | RESEARCH.md §6 | Paired bootstrap (10k resamples, not a t-test over seeds), Bonferroni correction, effect size, mean±std | IMPLEMENTED | `eval/stats.py::paired_bootstrap`, `bonferroni_correct`, `mean_std` | `config.BOOTSTRAP_RESAMPLES == 10_000`; paired resampling verified via `test_bootstrap_uses_the_pairing`; effect size (Cohen's d) reported alongside significance | None |
| R11 | RESEARCH.md §7.1.4 | Right-size with a stated power calculation, not a round number | IMPLEMENTED | `eval/gold_sets.py::proportion_ci_halfwidth`, `sizing_note` | **Used a hand-maintained `{0.90:1.6449,...}` z-value dict instead of the `scipy.stats.norm.ppf` the onboarding's own `[eval]` extras list names for exactly this purpose (§14: "scipy>=1.11 # bootstrap resampling / ECE binning").** `scipy` was added to `pyproject.toml` but never imported anywhere in the codebase — an unused dependency and a hand-rolled statistical primitive where a library exists, which RESEARCH.md §5 explicitly objects to for other metrics | MEDIUM — fixed in Step 6 |
| R12 | M6_ONBOARDING §14 | `[eval]` extras: `rouge-score`, `bert-score`, `scipy` | IMPLEMENTED | `pyproject.toml:78-82` | All three present | scipy was a phantom dependency until Step 6's fix |
| R13 | RESEARCH.md §7.1.3, PLAN.md §M6 | Duplicate only where subjective: 100% groundedness/relevance/entailment, 30% OOS, 0% repealed | IMPLEMENTED | `eval/config.py::IAA_DUPLICATION_RATE`, `eval/schemas.py::GOLD_SET_SPECS` | `test_iaa_duplication_rates_match_the_research_protocol` asserts the exact dict `{"retrieval":1.0,"groundedness":1.0,"entailment":1.0,"oos":0.30,"repealed":0.0}` | None |
| R14 | RESEARCH.md §7.1.2, M6_ONBOARDING §7 | TREC pooling: BM25/dense/hybrid top-10 → ~15 unique candidates/query | IMPLEMENTED (mechanism); NOT USED (data) | `eval/annotate/pooling.py` | `pool_candidates`, `pool_stats`, `unique_contribution` all implemented and tested with a worked judgment-budget calculation (`test_pool_stats_computes_the_judgment_budget` — asserts <3,500 judgments for 200 queries, matching RESEARCH.md's own "~3,000" claim) | `retrieval.jsonl`'s seed rows have empty `judgments` — the pooling code exists but was never run against a real corpus, because M2's real retrievers don't exist yet. Correctly disclosed in `eval/gold/PROVENANCE.md` |
| R15 | PLAN.md §M6, RESEARCH.md §7.2 | Annotation app: Streamlit, one item/screen, keyboard-driven, randomised order, blinded system labels, correction vs cold mode, logs rater id/timestamp/time-per-item, live κ/α, exports to `eval/gold/*.jsonl`, annotator-pool-agnostic | IMPLEMENTED | `eval/annotate/app.py`, `store.py`, `guidelines.py` | Every enforced property lives in a tested pure function: `blind_item` (strips system fields, tested against nested dicts), `randomised_order` (deterministic per-rater shuffle, tested as a permutation), `assign_mode` (hash-based ~10% cold, tested at 2000-sample scale for the fraction), `AnnotationStore.record` (timestamp + `seconds_on_item`, tested), `AnnotationStore.agreement()` (live κ/α), `export_gold_set` (writes straight to `eval/gold/*.jsonl`, tested for round-trip). Rater id is an opaque string everywhere; no field anywhere names a rater pool | None on the machinery. The **pilot itself was never run** (R16) — that is a human-annotator-time limitation, not a code gap |
| R16 | RESEARCH.md §7.3, PLAN.md §M6 | Pilot 20 items on one gold set before scaling; compute agreement; fix the rubric; record items/hour | NOT STARTED — BLOCKED | — | No pilot session exists; `AnnotationStore.throughput()` and `.agreement()` are ready to compute this the moment a human runs the app, but no human session has occurred | Cannot be completed by an AI agent alone; requires a live human rater. Disclosed in `eval/gold/PROVENANCE.md` and `docs/M6_COMPLETION_REPORT.md` §13. Not fixable in this audit |
| R17 | PLAN.md §M6, RESEARCH.md §7.1 | Five gold sets, 300–400 items each (200 queries for retrieval), collected via the app, multi-rater IAA | PARTIALLY IMPLEMENTED | `eval/gold/*.jsonl`, `eval/build_seed_gold.py` | 10–40 items per set exist, single-labelled (`n_annotations=1`), `agreement: null` throughout, every row carries a `provenance` string stating this plainly | This is the largest genuine gap in the milestone. The earlier completion report already disclosed it as a "limitation," which is correct, but the traceability status must be PARTIALLY IMPLEMENTED, not the informal framing used before |
| R18 | PLAN.md §M6 | `data/perturbations/`: seven-type taxonomy, from IN-Abs, releasable standalone, disjoint from every eval split | IMPLEMENTED | `data/perturbations/generate_perturbations.py` | All seven types implemented and unit-tested individually; `tests/test_perturbations.py::test_generator_is_releasable_standalone` asserts zero imports of `eval`/`smartlawai`/`data.`; disjointness from all five shipped gold sets is a **real, enforced assertion today** (not skipped) because the generator reads IN-Abs train-data and the seed gold sets were deliberately built from test-data (verified: 7,030 vs 100 doc-id stems, zero overlap) | None |
| R19 | RESEARCH.md §2.5, M1_ONBOARDING §9 convention | Train-split-only restriction, `pytest.skip`-guarded until `data/splits/*.json` exists | IMPLEMENTED (as a skip) | `tests/test_perturbations.py::test_perturbation_sources_are_drawn_from_the_training_split` | Skips with the exact stated reason ("data/splits/train.json does not exist yet"); confirmed by direct filesystem check that `data/splits/` does not exist | None — correctly honest, matches the sanctioned convention exactly |
| R20 | PLAN.md §M6 | Pull ClaimRAG-Law's 968 validated claims as external judge set | BLOCKED, documented | `eval/gold/external/PROVENANCE.md` | Checked the arXiv abstract, the full HTML (v4), and a web search; confirmed the release links were omitted for double-blind review and no public repository exists anywhere | Correctly follows M6_ONBOARDING §10's explicit instruction to state unavailability rather than substitute silently |
| R21 | M6_ONBOARDING §11 | `run_eval.py`: run `Pipeline.ask()` unmodified over gold items, compute every applicable metric, write `eval/results/{split}_{timestamp}.json` | IMPLEMENTED BUT INCORRECT (before fix) | `eval/run_eval.py` | `.ask()` genuinely used for `retrieval.jsonl` and `oos.jsonl` (R7 above). For `groundedness.jsonl`/`entailment.jsonl`/`repealed.jsonl`, no pipeline component was exercised at all before this audit's fix — the "applicable metric" was computed directly from the gold label, which is not "computing a metric," it is echoing the fixture | CRITICAL — fixed in Step 6: the verifier (a real pipeline component) is now exercised for the claim-level sets; the registry-dependent metrics correctly report `unavailable` since M4 does not exist |
| R22 | M6_ONBOARDING §11 | CI eval-regression gate on a fixed dev subset, generous threshold | IMPLEMENTED | `.github/workflows/eval-regression.yml`, `eval/run_eval.py::check_regression` | `--baseline` flag diffs against a committed JSON; flags both regressions past `REGRESSION_TOLERANCE=0.10` and metrics that disappeared entirely (a harness-breakage signal distinct from a quality regression) | None |
| R23 | Onboarding §2 "done when" | Full table computes end to end for the untrained system | IMPLEMENTED (now honestly so) | `eval/results/dev_*.json/.md`, `tests/test_eval_integration.py` | Runs with zero item failures; every metric is either a genuinely computed number or an `unavailable`/`not_applicable` with a stated reason — this is now true in the strict sense after Step 6's fix; before the fix, two sections contained a fabricated-looking (though not literally impossible) number | Re-verified after fixes (see compliance report §7) |
| R24 | CLAUDE.md I1 | Every retrieval call takes an explicit Scope | IMPLEMENTED | `eval/run_eval.py::EvalCorpus.scope_for` | `test_i1_holds_every_ask_carries_an_explicit_scope`; `Scope(doc_ids=(), ...)` raises `ValueError` and is exercised directly in the test | None |
| R25 | CLAUDE.md I2 | Safety components never fail open; `unavailable`, never a substituted number | PARTIALLY IMPLEMENTED (before fix) | `eval/metrics.py` (functions), `eval/run_eval.py` (call sites) | Every individual `MetricResult`/`AgreementResult` function is I2-correct in isolation (verified by dedicated tests for each). **The violation was one level up: `run_eval.py` fed those correct functions a fabricated input (the gold label standing in for a real system output), which is functionally the same failure I2 forbids — the number displayed was not measured, and nothing in the pipeline flagged that.** | CRITICAL — fixed in Step 6 |
| R26 | CLAUDE.md I3 | The refusal decision is application code, never an LLM | NOT M6's SCOPE | `src/smartlawai/pipeline.py::_decide` (unmodified) | M6 never touches `_decide`; `run_oos` only reads its output | Correctly out of scope |
| R27 | CLAUDE.md §3 repo layout | `eval/metrics.py`, `eval/gold/`, `eval/run_eval.py` at top level | IMPLEMENTED | `eval/` | Matches the diagram exactly | None |
| R28 | CLAUDE.md §4 | No magic numbers outside `config.py` | IMPLEMENTED (extended, not required verbatim) | `eval/config.py` | CLAUDE.md names one `config.py` under `src/smartlawai/`, for serving thresholds. `eval/config.py` is an inferred, not explicitly named, second file, created because the *convention* clearly generalises and no M6 doc contradicts it | Documented as an inference, not a literal quote, in `docs/M6_IMPLEMENTATION_ANALYSIS.md` §16 |
| R29 | CLAUDE.md §5 | Hallucination rate: ONE definition, claim-level, judge-based, used in serving+eval+paper | IMPLEMENTED BUT UNDERMINED (before fix) | `eval/metrics.py::hallucination_rate` | The function is a correct, exact complement of `groundedness_rate` (tested). **But `run_eval.py` was calling it on the gold LABEL, not a judge's output — which silently substitutes "the dataset's built-in answer" for "the judge's claim-level verdict," directly contradicting the one-definition rule, since the definition explicitly says "judge-based"** | CRITICAL — fixed in Step 6, now genuinely judge-based (the pipeline's verifier stands in for the not-yet-built HHEM/InLegalNLI) |
| R30 | OPS.md §4 | Every results file records `split_version` | NOT STARTED | `eval/run_eval.py::run` manifest | Manifest has `split` (the name, e.g. "dev") but no `split_version` field at all | MEDIUM — fixed in Step 6 (recorded as `null` with a stated reason, since `data/splits/*.json` doesn't exist yet, rather than omitted) |
| R31 | OPS.md §4 | `eval/results/*.json` kept forever, in git | IMPLEMENTED | `eval/results/dev_20260905T162402Z.{json,md}` committed (not gitignored) | Confirmed `.gitignore` has no rule matching `eval/results/` | None |
| R32 | OPS.md §8 | M6 is a fully standalone track; the eval harness "never imports the pipeline" (metric layer) | IMPLEMENTED | `tests/test_eval_integration.py::test_metric_layer_does_not_import_smartlawai`, `test_only_run_eval_imports_the_pipeline_class` | Static AST-based check across every file in `eval/`; only `run_eval.py` imports `smartlawai` | None |
| R33 | M4_ONBOARDING §6 | `RegistryResult{status: "in_force"\|"repealed"\|"superseded"\|"not_found", as_of, note}` is the actual contract M4 will emit | IMPLEMENTED BUT INCORRECT (before fix) | `eval/schemas.py::RepealedItem.as_registry_result` (invented shape: `resolved/exists/in_force/superseded_by`) | The invented shape does not match M4's documented contract at all — when M4 lands, a translation layer would be needed, which directly contradicts M6_ONBOARDING §8's stated goal of a caller that can plug real registry results straight in | CRITICAL (design defect, not a runtime bug) — fixed in Step 6 to match M4's exact `status`/`as_of`/`note` shape |
| R34 | RESEARCH.md T6 | Report a registry coverage rate; out-of-registry citations flagged, not silently passed | NOT STARTED | — | No distinct `registry_coverage_rate` metric existed before this audit | MEDIUM — added in Step 6 alongside the R33 fix |
| R35 | Image 1 (transcribed) | M6 has a prep phase (parallel with M0–M5: test sets, guidelines, ground truth, rating interface, scoring scripts) and a final-execution phase (needs working M2–M5) | IMPLEMENTED (prep phase only, correctly) | all of `eval/` | The prep-phase deliverables all exist; the final-execution phase is correctly not attempted, because M2–M5 don't exist yet | None — matches the documented phase boundary |
| R36 | Image 2 (transcribed) | Four-block decomposition: Evaluation Runner / Automatic Metrics / Human Evaluation / Statistical Analysis; deliverable includes an "evaluation report (tables + results)"; leakage checks "graceful skip if splits not available"; annotation app shows "guidelines & rating scales" | IMPLEMENTED | `run_eval.py` / `metrics.py`+`extractiveness.py` / `annotate/` / `stats.py`; `report.py` (JSON+Markdown); `tests/test_perturbations.py` skip; `annotate/guidelines.py` rendered in `app.py` | Module boundaries match the four blocks; report has both machine- and human-readable outputs; guidelines are data, not prose | None |
| R37 | M6_ONBOARDING §13 | Do not modify `pipeline.py`, `scope.py`, `trace.py`, `protocols.py`, `adapters/*`, `api/main.py`, `ui/app.py`; do not create stub `registry/`/`verify/` | IMPLEMENTED | — | `git status` / `git diff` confirm zero changes to any of these paths across the whole session, both before and after this audit's fixes | None |
| R38 | M6_ONBOARDING §13 | `tests/test_scope.py::test_cross_document_isolation` never deleted or skipped | IMPLEMENTED | `tests/test_scope.py` | Untouched; still present and passing | None |

## Summary counts

| Status | Count |
|---|---|
| IMPLEMENTED | 24 |
| IMPLEMENTED BUT INCORRECT (before this audit's fixes) | 6 (R4, R5, R21, R25, R29, R33) |
| PARTIALLY IMPLEMENTED | 4 (R6, R14, R17, R28-adjacent judgment call) |
| NOT STARTED / BLOCKED | 4 (R16, R20-documented-not-blocked-really, R30, R34) — R20 is resolved-as-documented, not truly blocked on M6's side |
| NOT REQUIRED (correctly out of scope) | 1 (R26) |

**Six requirements were marked IMPLEMENTED in the original completion report that
were actually IMPLEMENTED BUT INCORRECT.** All six are corrected in this audit
(see `docs/M6_GAP_ANALYSIS.md` for the fix detail and `docs/M6_COMPLIANCE_REPORT.md`
for post-fix verification). The remaining gaps (R16, R17, R20) are genuine
limitations of what a single AI-agent session can produce without live human
annotators, and are disclosed rather than hidden.
